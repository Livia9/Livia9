# 🧠 Resumo: Conceitos Fundamentais da Programação Orientada a Objetos (POO)

## 🔹 Classes
São moldes ou estruturas que definem as características (**atributos**) e comportamentos (**métodos**) de um tipo de objeto.  
**Exemplo:** uma classe `Carro` pode ter atributos como `cor`, `marca` e `velocidade`, e métodos como `acelerar()` ou `frear()`.

---

## 🔹 Objetos
São instâncias de uma classe. Ou seja, ao criar um objeto, estamos criando algo com base na estrutura definida pela classe.  
**Exemplo:** um objeto `meuCarro` do tipo `Carro`.

---

## 🔹 Atributos
São as variáveis que representam as características de um objeto.  
**Exemplo:** `cor`, `ano`, `modelo`.

---

## 🔹 Métodos
São funções dentro da classe que definem os comportamentos dos objetos.  
**Exemplo:** `ligarMotor()`, `mostrarDetalhes()`.

---

## 🔹 Abstração
É o princípio de esconder detalhes complexos e mostrar apenas o necessário.  
Na prática, significa focar no que um objeto **faz** e não em **como** ele faz internamente.

---

## 🔹 Encapsulamento
É o ato de proteger os dados do objeto, tornando atributos privados e permitindo acesso/controlado por meio de métodos públicos (`get` e `set`).  
Isso ajuda a manter a integridade e segurança dos dados.

---

## 🔹 Polimorfismo
Permite que uma mesma ação possa se comportar de maneiras diferentes dependendo do contexto.  
**Exemplo:** o método `exibirDados()` pode estar presente em várias classes e se comportar de forma diferente em cada uma delas.

---
